package com.neetry.platform.iam.domain.common.exception;

public enum ErrorCode {
    INVALID_PASSWORD_LENGTH,
    INVALID_CREDENTIALS,
    USER_NOT_FOUND,
    USER_EMAIL_EXISTS,
    INVALID_JWT_TOKEN,
    ILLEGAL_STATE,
    SERVER_ERROR
}
